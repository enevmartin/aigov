# aigov.bg — central configuration
# Switching the brain is a ONE-LINE change here. Nothing else moves.

brain: claude_code # claude_code | openclaw | api

brains:
  claude_code:
    # One "cabinet session" processes ALL pending tasks in a single batch run
    # (conserves the Claude Pro limit). Invoked by deploy/aigov-session.timer.
    cmd: claude
    output_format: json
  openclaw:
    # Skeleton — not implemented. See brains/openclaw/README.md.
    gateway: localhost:18789
  api:
    # Skeleton — not implemented. See brains/api/README.md.
    model: deepseek-v4-flash
    fallback: kimi-k2.6

# Each ministry may set `enabled: false` (declared but not running) and an
# optional `brain:` override — two ministries can run on different brains in
# the same cabinet session. Plain `- slug` is shorthand for enabled defaults.
# Пълният Министерски съвет (18 министерства, проверени срещу Административния
# регистър iisda.government.bg на 2026-07-20) + "премиерът" (government).
# Активни са само 3 — останалите са декларирани и готови, но не горят лимит.
ministries:
  - slug: finance
    enabled: true
    # brain: claude_code   # optional per-ministry override
  - slug: government # "премиерът" — composes joint reports from published/
    enabled: true
  - slug: health
    enabled: true
  - slug: environment
    enabled: true
  - slug: agriculture
    enabled: false
  - slug: economy
    enabled: false
  - slug: energy
    enabled: false
  - slug: innovation
    enabled: false
  - slug: tourism
    enabled: false
  - slug: foreign-affairs
    enabled: false
  - slug: interior
    enabled: false
  - slug: defense
    enabled: false

schedules:
  # Consumed by deploy/ systemd timers; documented here for one-glance clarity.
  ingest: hourly # aigov-ingest.timer  — collect data/news
  session: "twice-daily" # aigov-session.timer — cabinet session (brain runs)

paths:
  # All relative to the repo root. Core resolves them via core/config.py.
  tasks: tasks
  published: published
  data_raw: data/raw
  data_staging: data/staging
  ministries: ministries
